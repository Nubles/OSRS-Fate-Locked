# Vercel secret; never commit the real value.
DISCORD_APPLICATION_ID=
# Vercel secret; never commit the real value.
DISCORD_PUBLIC_KEY=
# Vercel secret; never commit the real value.
DISCORD_BOT_TOKEN=
# Public Fate Locked server ID.
DISCORD_GUILD_ID=1533446664709341357
# Vercel secret; never commit the real value.
DISCORD_ANNOUNCEMENTS_CHANNEL_ID=
# Vercel secret; never commit the real value.
DISCORD_RUN_JOURNALS_CHANNEL_ID=
# Vercel secret; never commit the real value.
DISCORD_VERIFICATION_QUEUE_CHANNEL_ID=
# Vercel secret; never commit the real value.
DISCORD_AUDIT_LOG_CHANNEL_ID=
# Vercel secret; never commit the real value.
DISCORD_RULES_CHANNEL_ID=
# Vercel secret; never commit the real value.
DISCORD_MODERATOR_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_ADMINISTRATOR_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_FATEKEEPER_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_VERIFIED_RUNNER_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_UPDATES_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_WEEKLY_SEED_ROLE_ID=
# Vercel secret; never commit the real value.
DISCORD_TAG_VANILLA_ID=
# Vercel secret; never commit the real value.
DISCORD_TAG_CHUNKED_ID=
# Vercel secret; never commit the real value.
DISCORD_TAG_CUSTOM_ID=
# Vercel secret; never commit the real value.
DISCORD_TAG_ACTIVE_ID=
# Vercel secret; never commit the real value.
DISCORD_TAG_VERIFIED_ID=
# Vercel secret; never commit the real value.
DISCORD_COMPONENT_HMAC_KEY=
# Vercel secret; never commit the real value.
AUTOMATION_HMAC_KEY=
# Vercel secret; never commit the real value.
AUTOMATION_ALLOWED_REPOSITORIES=
# Safety switch. Leave false unless a reviewed deployment explicitly enables mutations.
DISCORD_MUTATIONS_ENABLED=false
