# Task 5 report — Discord content publication

- Date: 2026-08-02
- Server: Fate Locked Ironman
- Result: pass
- Secrets, private URLs, invites, and message identifiers recorded: no
- Paid features purchased: no

## Channel topics

All 20 text, announcement, and forum channels received the source topic. Each saved value was reopened in Discord and compared with `server-content.md`. Forum post guidelines retain the exact short topic followed by the approved forum instructions and starter template.

## Notice Board publication

- `#welcome` contains one primary message combining `fate-locked-community-header.png` with the exact welcome opening. The attachment, tracker link, RuneLite guide link, web source link, RuneLite source link, and Jagex disclaimer were visible after sending.
- `#rules-and-safety` contains the complete rules entries and verification explanation from the source.
- `#roles-and-pings` contains the exact identity, recognition, and notification role descriptions.
- `#announcements` contains the pre-launch notice and explicitly states that the public invite is not open yet.

## Forum configuration

All three forums use list view and require at least one tag.

- `#run-journals`: member tags `Vanilla`, `Chunked`, `Custom`, `Active`, `Completed`, and `Archived`; staff-only `Verified`; approved title format, run guidance, and starter template.
- `#support-desk`: member tags `Tracker`, `RuneLite`, `Rules`, `Bug`, and `Verification`; staff-only `Needs Info` and `Resolved`; approved public-verification warning and support template.
- `#ideas-and-feedback`: member tags `Suggestion`, `Balance`, and `Content`; staff-only `Under Review`, `Accepted`, and `Declined`; approved idea template and decline guidance.

The `STAFF TEST - delete before launch` run-journal post proved that posting remained disabled before tags were selected and became enabled after one run-type tag and one state tag were selected. Only that clearly labelled post was deleted, and a fresh forum view confirmed it was absent.

## Pin and boundary checks

Discord's pinned-message view showed exactly one primary pin in each of `#welcome`, `#rules-and-safety`, and `#roles-and-pings`. `#announcements` showed no pinned messages, so the pre-launch notice is not pinned.

Task 5 did not configure Rules Screening, onboarding, AutoMod, webhooks, or invites. It did not access other servers, direct messages, authentication settings, or paid features.
