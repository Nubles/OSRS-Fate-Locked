# RuneProof requirements at a glance

Maintainer direction: make missing unlocks easy to spot while retaining the simpler guide.

Add a compact Still needed section below the readiness sentence, before progress and the walkthrough. Show each outstanding requirement once with a readable label, icon, and a link to its affected step. Use warm amber for known requirements, plain item labels for supplies, and a separately labelled Needs checking state for uncertainty. Keep the detailed step warnings and confirmation guards intact.

Derive the summary from the current guide model in a pure helper, including equipment completion guards, structured gates, locked areas, and unresolved locations or travel. Ignore completed steps, except that an unmet final equipment guard must remain represented when its earlier wear step was checked. Deduplicate the final completion copy when an earlier remaining direct gate already explains it. Never turn missing evidence into a definite unlock requirement or infer quest readiness from an empty list.

The section disappears when the model has no remaining needs. Links scroll and focus the relevant instruction without completing it. No new game facts, save changes, or public deployment. Verification uses the materialized Vanilla Restless Ghost account at Neck T0/T1, summary deduplication and jump behavior, type checking, production build, and desktop/mobile preview checks. Chunked tests remain out of scope.
