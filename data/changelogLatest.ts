/**
 * The newest What's New release id, kept apart from the release notes so the
 * app can decide whether What's New opens without loading them.
 * data/changelog.test.ts pins it to CHANGELOG_RELEASES[0]; update it whenever
 * a release is added.
 */
export const LATEST_CHANGELOG_ID = '2026-09-25-herblore-sailing-quests';
